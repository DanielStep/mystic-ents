package model.traits;
/**
 * Class that extends Trait that represents the range in which a piece can perform actions
 * @author Daniel
 *
 */
public class RangeTrait extends Trait {

	public RangeTrait(int startingValue) {
		super(startingValue);
	}
	
}